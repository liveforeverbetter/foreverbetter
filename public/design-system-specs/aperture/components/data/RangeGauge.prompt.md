Horizontal range gauge with a hatched optimal band and a marker. For load/index readouts.

```jsx
<RangeGauge status="Balanced" statusTone="good" value={0.55} band={[0.4, 0.7]}
  leftLabel="Under" rightLabel="Injury risk" />
```
`value` and `band` are 0–1 positions across the track.
