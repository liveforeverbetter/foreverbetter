Status pill/badge for readouts. `tone` matches the reading; `soft` (default) for in-card status, `solid` for high-emphasis, `outline` for quiet.

```jsx
<StatusPill tone="good">Good</StatusPill>
<StatusPill tone="attention" variant="solid">Out of range</StatusPill>
```
