Tab switcher — `underline` (default) or `segmented`.

```jsx
<Tabs value={t} onChange={setT} variant="segmented"
  items={[{value:'day',label:'Day'},{value:'week',label:'Week'},{value:'month',label:'Month'}]} />
```
