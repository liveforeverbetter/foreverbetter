Empty/zero-data state.

```jsx
<EmptyState icon={<Icon name="calendar-plus"/>} title="No activities yet"
  description="Log a workout or let auto-detect find one." action={<Button>Add activity</Button>} />
```
