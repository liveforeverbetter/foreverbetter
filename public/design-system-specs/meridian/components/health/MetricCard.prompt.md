Ring in a glowing card with a trend delta. The three-up hero tiles on the dashboard.

```jsx
<MetricCard channel="recovery" value={65} unit="%" label="Recovery" delta="7%" deltaTone="down" note="vs. baseline" />
```
