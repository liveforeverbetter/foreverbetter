Circular portrait; falls back to a monogram on a tonal gradient. `ring` adds a brand halo (e.g. current user).

```jsx
<Avatar name="Blake Sun" size={40} tone="warm" />
<Avatar src={photo} name="Blake Sun" ring />
```
