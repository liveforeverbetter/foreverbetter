Single-select. Prefer `RadioGroup` for a bound set.

```jsx
<RadioGroup value={pace} onChange={setPace}
  options={[{value:'slow',label:'Slow',description:'Sustainable'},{value:'fast',label:'Fast'}]} />
```
