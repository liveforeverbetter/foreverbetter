Labeled range with an optional tick gauge — the pace-of-aging / zone control.

```jsx
<Slider value={pace} min={-1} max={3} step={0.1} ticks={40}
  valueLabel="0.2x" leftLabel="Slow" rightLabel="Fast" onChange={setPace} />
```
