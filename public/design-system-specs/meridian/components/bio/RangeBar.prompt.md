The signature biomarker reference-range visual. Colored zones with a value marker.

```jsx
<RangeBar value={82} min={70} max={100} optimalLow={74} optimalHigh={99} valueLabel="82 mg/dL" showScale />
```
Pass `zones` for a custom multi-band model.
